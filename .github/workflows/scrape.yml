name: Rumble Video Scraper

on:
  schedule:
    # Her gün Türkiye saati ile 03:00'te çalışır (Cron UTC bazlıdır)
    - cron: "0 0 * * *" 
  workflow_dispatch: # GitHub panelinden elle çalıştırmanızı sağlar

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Repo'yu Çek
        uses: actions/checkout@v3

      - name: Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: Gerekli Kütüphaneleri Yükle
        run: pip install requests

      - name: Scripti Çalıştır (Video Çek)
        run: python scrape.py

      - name: Değişiklikleri Gönder (Commit & Push)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add videos.json
          # Eğer değişiklik yoksa hata vermemesi için "|| true" eklendi
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 Auto update: Video listesi güncellendi"
          git push
