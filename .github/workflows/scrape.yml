name: Rumble Video Scraper

# --- BURASI ÇOK ÖNEMLİ ---
# Bu satır olmadan Actions repo'ya dosya yazamaz (push yapamaz)
permissions:
  contents: write
# -------------------------

on:
  schedule:
    # Her gün saat 00:00'da çalışır
    - cron: "0 0 * * *" 
  workflow_dispatch: # Elle tetiklemek için

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Repo'yu Çek
        uses: actions/checkout@v3

      - name: Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: Kütüphaneleri Yükle
        run: pip install requests

      - name: Scripti Çalıştır (Scrape)
        run: python scrape.py

      - name: Değişiklikleri Gönder (Commit & Push)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add videos.json
          # Eğer bir değişiklik yoksa (dosya aynıysa) commit yapma, yoksa yap ve pushla
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 Auto update: Video listesi güncellendi"
          git push
