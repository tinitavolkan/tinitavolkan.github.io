name: Rumble Video Scraper (Simple)

permissions:
  contents: write

on:
  schedule:
    # Her gün
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Repo'yu Çek
        uses: actions/checkout@v3

      - name: Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: Gerekli Kütüphaneyi Yükle (Sadece requests)
        run: pip install requests

      - name: Scripti Çalıştır
        run: python scrape.py

      - name: Değişiklikleri Gönder (Commit & Push)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add videos.json
          # Değişiklik yoksa hata verme, varsa commit at
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 Auto update: Video listesi güncellendi"
          git push
